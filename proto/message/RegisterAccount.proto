syntax = "proto3";

message ReqRegAccount
{
	string account = 1;
	string password = 2;
}

enum RegisterError
{
	kRegSuccess = 0;
	kAcountRepeat = 1;	// 账号重复
	kAcountIllega = 2;	// 账号非法
}

message RspRegAccount
{
	RegisterError status = 1;
}
